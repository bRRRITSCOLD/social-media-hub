// node_modules
import { makeStyles } from '@material-ui/core/styles';

const useAppStyles = makeStyles({
  root: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
  },
});

export { useAppStyles };
