// node_modules
import { makeStyles } from '@material-ui/core/styles';

const useTwitterPostStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  media: {
    height: 140,
  },
});

export { useTwitterPostStyles };
